fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg[binary]>=3.2      # Postgres driver (Supabase); not needed for local SQLite
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
pyjwt>=2.8
httpx>=0.27               # also used for Groq AI extraction (optional at runtime: rule-based fallback without a key)
firebase-admin>=6.5       # FCM pushes; inactive until FIREBASE_CREDENTIALS is set

# --- dev / test ---
pytest>=8.0
